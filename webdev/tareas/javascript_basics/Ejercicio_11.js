/*
Escribe una función que tome una lista de cadena de textos 
y devuelva una nueva lista con todas las cadenas en orden alfabético.
*/

function sortStrings(arr) {
    return arr.sort();
}

console.log(sortStrings(['bebe', 'camino', 'arbol', 'zorro']));